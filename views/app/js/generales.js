// Funcion para evitar que cada ves que tenga que tomar un elemnto
// por su ID no tener que escribir document.getElementById(), simplemente escribo
// __(id) con el id y esta me la devuelve.
function __(id){
  return document.getElementById(id);
}
